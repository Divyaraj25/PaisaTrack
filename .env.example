# Environment variables for PaisaTrack Application

# MongoDB Atlas Connection String
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority
MONGO_URI=MONGO_URI

# Flask Secret Key - Used for session encryption
# Generate a strong secret key using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Flask Environment
FLASK_ENV=production

# Additional environment variables can be added here as needed